{
  "name": "home",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "VITE_LAST_COMMIT_DATE=$(git log -1 --format=%cs 2>/dev/null || echo \"No commits yet\") vite",
    "build": "VITE_LAST_COMMIT_DATE=$(git log -1 --format=%cs 2>/dev/null || echo \"No commits yet\") vite build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@tsconfig/svelte": "^5.0.8",
    "@types/node": "^24.12.2",
    "svelte": "^5.55.1",
    "svelte-check": "^4.4.6",
    "typescript": "~6.0.2",
    "vite": "^8.0.4"
  }
}
